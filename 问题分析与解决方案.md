# 问题分析与解决方案

## 问题总结

### 问题一：AI只输出一条日志，无法读取表格数据
**现象**：表格有100多行数据，但AI只生成一条日志

### 问题二：AI输出包含无关内容
**现象**：
```
# 炽霞日志
好的，这是为角色"炽霞"生成的历史事件记录：
好感度：65
惊蛰后廿七日 我从巡尉营里听说了...
```

### 问题三：缺少停止按钮
**现象**：AI识别出9个角色后不断输出，无法手动停止

---

## 原因分析

### 问题一：表格数据未传入AI
**根本原因**：
1. ❌ 当前代码没有调用 `st-memory-enhancement` 插件的API来获取表格数据
2. ❌ 提示词中没有包含表格数据
3. ❌ AI只能看到最近几条聊天消息，无法感知完整表格内容

**参考项目的正确做法**：
```javascript
// 1. 调用表格插件API获取数据
if (window.stMemoryEnhancement && typeof window.stMemoryEnhancement.ext_exportAllTablesAsJson === 'function') {
    const tableDataJson = window.stMemoryEnhancement.ext_exportAllTablesAsJson();
    tableDataContent = formatTableDataForLLM(tableDataJson);
}

// 2. 格式化为适合LLM阅读的文本
function formatTableDataForLLM(jsonData) {
    // 将表格JSON转换为清晰的文本格式
    // 包含表头、每一行数据的键值对
}

// 3. 将表格数据注入到提示词中
const prompt = `${userMessage}\n\n${tableDataContent}`;
```

### 问题二：提示词不够严格
**根本原因**：
1. ❌ 提示词没有明确要求"只输出事件记录"
2. ❌ 没有使用XML标签约束输出格式
3. ❌ AI会添加"好的，这是..."等礼貌用语

**改进方向**：
```
你的输出必须严格遵循以下格式，不要包含任何其他内容：
YYYYMMDD 事件描述

禁止输出：
- "好的"、"这是"等开场白
- "好感度"等额外信息
- 任何格式说明或解释

如果没有重要事件，只输出：无
```

### 问题三：缺少中断机制
**根本原因**：
1. ❌ 没有全局停止标志（如 `isProcessing` 变量）
2. ❌ UI没有提供停止按钮
3. ❌ 循环处理时无法被中断

---

## 解决方案

### 方案一：集成表格数据读取 ⭐⭐⭐⭐⭐

**实施步骤**：

1. **在 `information_aggregator.js` 中添加表格数据读取函数**
```javascript
/**
 * 从 st-memory-enhancement 插件读取表格数据
 * @returns {string} 格式化后的表格文本
 */
function getTableData() {
    try {
        if (window.stMemoryEnhancement && 
            typeof window.stMemoryEnhancement.ext_exportAllTablesAsJson === 'function') {
            const tableJson = window.stMemoryEnhancement.ext_exportAllTablesAsJson();
            return formatTableDataForLLM(tableJson);
        } else {
            return '未检测到记忆增强插件或版本不兼容';
        }
    } catch (error) {
        logger.error(`[信息聚合] 读取表格数据失败: ${error.message}`);
        return '读取表格数据时发生错误';
    }
}

/**
 * 格式化表格数据为LLM可读文本
 */
function formatTableDataForLLM(jsonData) {
    if (!jsonData || typeof jsonData !== 'object' || Object.keys(jsonData).length === 0) {
        return '当前无任何可用的表格数据。';
    }

    let output = '以下是角色记忆表格数据：\n\n';

    for (const sheetId in jsonData) {
        const sheet = jsonData[sheetId];
        if (sheet && sheet.name && sheet.content && sheet.content.length > 1) {
            output += `## 表格: ${sheet.name}\n`;
            const headers = sheet.content[0].slice(1);
            const rows = sheet.content.slice(1);

            rows.forEach((row, rowIndex) => {
                const rowData = row.slice(1);
                let rowOutput = '';
                let hasContent = false;
                
                headers.forEach((header, index) => {
                    const cellValue = rowData[index];
                    if (cellValue !== null && cellValue !== undefined && String(cellValue).trim() !== '') {
                        rowOutput += `  - ${header}: ${cellValue}\n`;
                        hasContent = true;
                    }
                });

                if (hasContent) {
                    output += `\n### 记录 ${rowIndex + 1}\n${rowOutput}`;
                }
            });
        }
    }
    
    output += '\n--- 表格数据结束 ---\n';
    return output;
}
```

2. **在 `collectWorldState()` 中调用表格数据**
```javascript
export function collectWorldState() {
    // ... 现有代码 ...
    
    // 添加表格数据
    const tableData = getTableData();
    if (tableData) {
        sections.push({
            title: "角色记忆表格",
            content: tableData,
            priority: 2  // 高优先级
        });
    }
    
    // ... 现有代码 ...
}
```

**优点**：
- ✅ AI能看到所有100+行表格数据
- ✅ 不需要修改现有架构
- ✅ 自动适配表格插件

---

### 方案二：优化提示词格式控制 ⭐⭐⭐⭐⭐

**实施步骤**：

1. **修改默认提示词（在 `settingsManager.js` 中）**
```javascript
diaryPrompt: `你是一个专业的事件记录员。请分析下面的对话内容和记忆表格，为角色"\${characterName}"生成一条精炼的历史事件记录。

<important_rules>
1. 你的输出必须严格遵循格式：YYYYMMDD 事件描述
2. 禁止输出任何其他内容，包括：
   - 开场白（如"好的"、"这是"）
   - 额外信息（如"好感度"）
   - 任何解释或说明
3. 如果确实没有重要事件，只输出：无
</important_rules>

<memory_context>
以下是角色的历史记忆表格，包含所有已记录的事件：
\${tableData}
</memory_context>

<recent_dialogue>
最近的对话内容：
\${recentMessages}
</recent_dialogue>

请基于以上信息，生成一条新的事件记录：`,
```

2. **在 `character_diary.js` 中传入表格数据**
```javascript
export async function addCharacterDiary(characterName) {
    // ... 现有代码 ...
    
    // 获取表格数据
    const tableData = getTableData();
    
    // 构建提示词
    const prompt = settings.diaryPrompt
        .replace(/\$\{characterName\}/g, characterName)
        .replace(/\$\{tableData\}/g, tableData)
        .replace(/\$\{recentMessages\}/g, recentMessages);
    
    // ... 现有代码 ...
}
```

**优点**：
- ✅ 输出格式更规范
- ✅ 减少无关内容
- ✅ AI能参考历史记录避免重复

---

### 方案三：添加停止按钮 ⭐⭐⭐⭐⭐

**实施步骤**：

1. **在 `character_diary.js` 中添加停止标志**
```javascript
// 在文件顶部添加
let isDiaryProcessing = false;
let shouldStopDiary = false;

/**
 * 停止日志生成
 */
export function stopDiaryGeneration() {
    shouldStopDiary = true;
    mainLogger.warn("用户请求停止日志生成");
}

export async function addCharacterDiary(characterName) {
    if (isDiaryProcessing) {
        mainLogger.warn("日志生成正在进行中，请勿重复触发");
        return false;
    }
    
    isDiaryProcessing = true;
    shouldStopDiary = false;
    
    try {
        // 在API调用前检查
        if (shouldStopDiary) {
            mainLogger.info("日志生成已被用户停止");
            return false;
        }
        
        // ... 现有代码 ...
        
    } finally {
        isDiaryProcessing = false;
        shouldStopDiary = false;
    }
}
```

2. **在日志面板HTML中添加停止按钮（modal_settings.html）**
```html
<div class="ape_log_controls">
    <button id="ape_clear_log_button" class="menu_button">
        <i class="fas fa-trash-alt"></i> 清空日志
    </button>
    <button id="ape_stop_diary_button" class="menu_button" style="display: none;">
        <i class="fas fa-stop-circle"></i> 停止生成
    </button>
</div>
```

3. **在 `settingsManager.js` 中绑定停止按钮**
```javascript
import { stopDiaryGeneration } from './character_diary.js';

// 在 initializeSettings 函数中添加
const stopDiaryButton = document.getElementById('ape_stop_diary_button');
if (stopDiaryButton) {
    stopDiaryButton.addEventListener('click', () => {
        stopDiaryGeneration();
        stopDiaryButton.style.display = 'none';
        toastr.warning("正在停止日志生成...", "角色日志");
    });
}

// 修改生成按钮逻辑，显示停止按钮
addDiaryButton.addEventListener('click', async () => {
    // ... 现有代码 ...
    
    // 显示停止按钮
    if (stopDiaryButton) {
        stopDiaryButton.style.display = 'inline-block';
    }
    
    try {
        const success = await addCharacterDiary(characterName);
        // ... 现有代码 ...
    } finally {
        // 隐藏停止按钮
        if (stopDiaryButton) {
            stopDiaryButton.style.display = 'none';
        }
    }
});
```

**优点**：
- ✅ 用户可随时中断
- ✅ 防止资源浪费
- ✅ 更好的用户体验

---

## 实施优先级

1. **立即实施（P0）**：
   - ✅ 方案一：集成表格数据读取（解决核心问题）
   - ✅ 方案二：优化提示词（提高输出质量）

2. **尽快实施（P1）**：
   - ✅ 方案三：添加停止按钮（改善体验）

---

## 预期效果

实施后：
1. ✅ AI能看到所有100+行表格数据，生成更准确的日志
2. ✅ 输出格式规范，不含无关内容
3. ✅ 用户可手动停止生成，防止失控

---

## 风险评估

**低风险**：
- 所有修改都基于参考项目的成熟方案
- 不破坏现有功能
- 向后兼容（如果表格插件不存在，会优雅降级）

**需要测试**：
1. 表格数据格式是否正确
2. Token数是否超限（100行数据可能很长）
3. 停止按钮响应速度

---

## 等待确认

请确认是否同意以上方案，我会按顺序实施：
1. ✅ 添加表格数据读取功能
2. ✅ 优化提示词和输出格式控制
3. ✅ 添加停止按钮

如有任何疑问或需要调整，请告知！
