# SillyTavern-AutoPlotEngine 修复说明

## 问题诊断

你的扩展在插入世界书时失败，主要有以下问题：

### 1. 世界书写入问题 (plot_engine.js)
- **原问题**：使用了不存在的 `TavernHelper.getLorebookList()`, `TavernHelper.createLorebook()` 等 API
- **原因**：这些 API 在标准 SillyTavern 中并不存在或不可靠
- **解决方案**：改用 SillyTavern 原生 API

### 2. 世界书读取问题 (information_aggregator.js)  
- **原问题**：直接使用 `TavernHelper` 读取世界书，没有错误处理
- **原因**：`TavernHelper` 可能不存在或版本不兼容
- **解决方案**：添加兼容性检查，提供原生 API 后备方案

## 修复详情

### plot_engine.js 修改

#### 导入更改
```javascript
// 移除
const { TavernHelper, toastr } = window;

// 新增
import { loadWorldInfo, createNewWorldInfo, saveWorldInfo, world_names, createWorldInfoEntry } from "/scripts/world-info.js";
import { eventSource, event_types } from "/script.js";
const { toastr } = window;
```

#### updatePlotLorebook() 函数重写
使用正确的原生 API 流程：

1. **检查世界书是否存在**
   ```javascript
   if (!world_names.includes(LOREBOOK_NAME)) {
       await createNewWorldInfo(LOREBOOK_NAME);
   }
   ```

2. **加载世界书数据**
   ```javascript
   const bookData = await loadWorldInfo(LOREBOOK_NAME);
   ```

3. **查找或创建条目**
   ```javascript
   const existingEntry = Object.values(bookData.entries || {}).find(
       entry => entry.comment === ENTRY_COMMENT && !entry.disable
   );
   ```

4. **设置正确的条目属性**
   ```javascript
   {
       comment: "自动剧情大纲 - 时间戳",
       content: outlineContent,
       key: ["剧情", "plot"],
       constant: true,      // 蓝灯 - 始终激活
       position: 0,         // before_char
       depth: 1,            // 扫描深度为1
       disable: false,
       order: 1000          // 高优先级
   }
   ```

5. **保存世界书**
   ```javascript
   await saveWorldInfo(LOREBOOK_NAME, bookData, true);
   ```

### information_aggregator.js 修改

#### 新增导入
```javascript
import { characters } from '/script.js';
import { loadWorldInfo, world_names } from '/scripts/world-info.js';
```

#### 添加兼容性检查函数
```javascript
function isTavernHelperAvailable() {
    return typeof window.TavernHelper !== 'undefined' && 
           window.TavernHelper !== null &&
           typeof window.TavernHelper.getLorebookEntries === 'function';
}
```

#### getActiveLorebooks() 重写
实现双重策略：
1. **优先使用 TavernHelper**（如果可用）
2. **后备使用原生 API**（保证兼容性）

```javascript
// 尝试 TavernHelper
if (isTavernHelperAvailable()) {
    try {
        entries = await window.TavernHelper.getLorebookEntries(bookName);
    } catch (error) {
        // 失败则使用原生 API
    }
}

// 后备方案
if (entries.length === 0) {
    const bookData = await loadWorldInfo(bookName);
    // 手动提取条目...
}
```

## 关键技术点

### 1. 世界书 API 对比

| 功能 | TavernHelper (不可靠) | 原生 API (推荐) |
|------|---------------------|----------------|
| 获取世界书列表 | `TavernHelper.getLorebookList()` | `world_names` 数组 |
| 创建世界书 | `TavernHelper.createLorebook()` | `createNewWorldInfo(name)` |
| 加载世界书 | - | `loadWorldInfo(name)` |
| 读取条目 | `TavernHelper.getLorebookEntries()` | 从 `bookData.entries` 读取 |
| 创建条目 | `TavernHelper.createLorebookEntries()` | `createWorldInfoEntry()` + 手动配置 |
| 保存世界书 | - | `saveWorldInfo(name, data, true)` |

### 2. 世界书条目关键属性

- `constant: true` - 蓝灯模式，始终激活
- `position: 0` - before_char，在角色定义之前插入
- `depth: 1` - 扫描深度为1（按计划要求）
- `disable: false` - 启用条目
- `order: 1000` - 高优先级
- `key: []` - 触发关键词数组

### 3. UI 刷新机制

创建新世界书后需要触发 UI 刷新：
```javascript
if (eventSource && typeof eventSource.emit === "function") {
    eventSource.emit(event_types.CHARACTER_PAGE_LOADED);
}
```

## 测试建议

1. **安装扩展到 SillyTavern**
2. **配置 API 设置**（在扩展设置面板）
3. **开始聊天**（发送足够消息触发阈值）
4. **检查控制台日志**：
   - 查看 `[AutoPlotEngine]` 日志
   - 确认世界书创建成功
   - 确认条目插入成功
5. **验证世界书界面**：
   - 打开世界书管理
   - 查看是否出现"宏观剧情"世界书
   - 检查条目内容和设置

## 兼容性

- ✅ 支持标准 SillyTavern 安装
- ✅ 兼容有/无 TavernHelper 的环境
- ✅ 使用稳定的原生 API
- ✅ 完整的错误处理和日志

## 参考项目对比

你的项目现在采用了与参考项目类似的方案：
- **读取**：优先 TavernHelper，后备原生 API
- **写入**：完全使用原生 API（参考 Amily2 项目的 lore.js）
- **兼容性**：参考 tavernhelper-compatibility.js 的双重策略

---

## 最新修复 (2025/10/13)

### 问题：世界书创建位置错误

**症状：**
- 日志显示"世界书已更新"，但条目没有出现在角色卡的世界书中
- 创建了独立的全局世界书文件（如"宏观剧情.json"）
- UI 中找不到对应的条目

**根本原因：**
使用 `createNewWorldInfo()` 创建独立世界书，而不是写入当前角色卡的主世界书。

**解决方案：使用 TavernHelper API**

1. **引入 TavernHelper**：
   ```javascript
   const { toastr, TavernHelper } = window;
   ```

2. **获取角色卡主世界书**：
   ```javascript
   const LOREBOOK_NAME = await TavernHelper.getCurrentCharPrimaryLorebook();
   
   if (!LOREBOOK_NAME) {
       throw new Error("当前角色没有绑定世界书，请先为角色卡设置主世界书");
   }
   ```

3. **在角色卡世界书中操作**：
   ```javascript
   // 加载角色卡的世界书
   const bookData = await loadWorldInfo(LOREBOOK_NAME);
   
   // 查找现有条目
   const existingEntry = Object.values(bookData.entries || {}).find(
       entry => entry.comment && entry.comment.startsWith(ENTRY_COMMENT)
   );
   
   if (existingEntry) {
       // 更新现有条目
       existingEntry.content = outlineContent;
   } else {
       // 在角色卡世界书中创建新条目
       const newEntry = createWorldInfoEntry(LOREBOOK_NAME, bookData);
       Object.assign(newEntry, {
           comment: `${ENTRY_COMMENT} - ${new Date().toISOString()}`,
           content: outlineContent,
           key: ["剧情", "plot", "大纲", "故事", "情节"],
           constant: true,
           disable: false
       });
   }
   
   // 保存
   await saveWorldInfo(LOREBOOK_NAME, bookData, true);
   ```

**关键点：**
- 不再调用 `createNewWorldInfo()`，避免创建独立世界书
- 使用 `TavernHelper.getCurrentCharPrimaryLorebook()` 获取角色卡绑定的世界书
- 在该世界书中直接创建/更新条目
- 剧情大纲会跟随角色卡，而不是全局独立存在

**使用前提：**
用户必须先为角色卡绑定一个主世界书（在角色卡设置中操作）。

**参考来源：**
参考项目 `CharacterWorldBook/` 目录中的实现方式。

---

## 下一步

现在你的扩展应该能够：
1. ✅ 正常读取世界书内容
2. ✅ 正常调用 API 生成剧情大纲
3. ✅ 正确写入角色卡的主世界书（而不是创建独立世界书）
4. ✅ 正常插入剧情大纲条目到角色卡世界书

如果还有问题，请检查浏览器控制台的详细日志输出。
