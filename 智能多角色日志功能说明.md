# 智能多角色日志功能说明

## 📋 功能概述

新增的**智能多角色识别**功能允许扩展自动检测对话中的所有AI角色，并为每个角色独立生成日志记录。这对于群聊场景特别有用。

## ✨ 新增功能

### 1. 智能多角色识别
- **自动检测**：从最近的对话中自动识别所有参与的AI角色
- **批量生成**：一次性为所有检测到的角色生成日志
- **智能过滤**：自动排除用户消息和系统消息，只处理AI角色

### 2. 静默模式
- **无打扰运行**：启用后关闭所有toastr弹窗通知
- **后台运行**：适合自动模式，不影响用户体验
- **日志记录**：所有操作仍会记录在控制台日志中

## 🎯 使用场景

### 场景1：群聊对话
当你与多个角色同时对话时：
```
角色A：你好！
角色B：很高兴见到你！
角色C：今天天气不错。
```
智能识别会自动为A、B、C三个角色分别生成日志。

### 场景2：切换角色对话
在单次会话中切换不同角色时，每个角色的经历都会被独立记录。

## ⚙️ 配置说明

### 基本设置（角色日志标签页）

#### 1. 启用角色日志
- 总开关，关闭后所有日志功能不运行

#### 2. 运行模式
- **自动模式**：每N条消息后自动触发
- **手动模式**：需要手动点击按钮生成

#### 3. 自动触发阈值
- 默认：15条消息
- 范围：5-50条
- 建议：群聊场景可以设置更大的值（如30-40）

#### 4. 目标角色
- 单角色模式下使用
- 留空则使用当前对话角色
- **智能识别启用时此项无效**

#### 5. ⭐ 智能多角色识别（新功能）
- **默认：启用**
- 启用后自动识别所有角色并批量生成日志
- 禁用后回到单角色模式（使用"目标角色"设置）

#### 6. 存储位置
- **追加模式**：在原角色条目末尾追加"历史事件"部分
- **独立模式**：创建新条目"角色名日志"，触发词为角色名

### 核心设置（核心设置标签页）

#### 静默模式（新功能）
- **默认：关闭**
- 启用后关闭所有弹出通知
- 仅在控制台显示日志
- 适合后台自动运行

## 📖 使用流程

### 自动模式 + 智能识别（推荐）

1. **启用功能**
   ```
   ✓ 启用角色日志
   ✓ 运行模式：自动
   ✓ 智能多角色识别：启用
   ```

2. **配置阈值**
   ```
   自动触发阈值：15-30条（根据对话频率调整）
   ```

3. **选择存储模式**
   ```
   - 追加模式：适合已有角色信息条目的情况
   - 独立模式：为每个角色创建独立日志条目
   ```

4. **可选：启用静默模式**
   ```
   如果不想看到频繁的通知弹窗，可在"核心设置"中启用静默模式
   ```

5. **开始对话**
   - 扩展会自动监控消息数量
   - 达到阈值后自动触发批量日志生成
   - 所有检测到的角色都会生成日志

### 手动模式

1. **启用功能**
   ```
   ✓ 启用角色日志
   运行模式：手动
   ```

2. **手动生成**
   - 在"角色日志"标签页
   - 输入角色名称
   - 点击"立即生成日志"按钮

## 📊 批量生成结果

智能多角色模式会输出详细的统计信息：

```
[批量日志] ========== 批量生成完成 ==========
[批量日志] 总计: 3 | 成功: 2 | 跳过: 1 | 失败: 0

✓ 角色A - 成功
✓ 角色B - 成功  
○ 角色C - 跳过（无重要事件）
```

### 结果说明
- **成功**：成功生成并保存了日志
- **跳过**：该角色在此次对话中没有重要事件
- **失败**：生成过程出错（会显示错误原因）

## 🔍 工作原理

### 角色识别逻辑
```javascript
1. 获取最近20条消息
2. 遍历每条消息
3. 排除用户消息（is_user === true）
4. 排除系统消息（name === 'System' 或 '系统'）
5. 排除特殊标记（name以'['开头）
6. 收集所有有效的角色名称
7. 返回去重后的角色列表
```

### 批量生成流程
```javascript
1. 检测所有角色
2. 依次为每个角色调用日志生成API
3. 每次调用间隔1秒（避免API限流）
4. 使用静默模式（不弹窗）
5. 汇总统计结果
6. 输出最终报告
```

## 💡 最佳实践

### 1. 群聊场景
```
✓ 智能多角色识别：启用
✓ 自动触发阈值：30条（群聊消息多）
✓ 静默模式：启用（避免频繁通知）
✓ 存储模式：独立模式（便于管理）
```

### 2. 单角色场景
```
智能多角色识别：禁用
目标角色：留空或指定角色名
自动触发阈值：15条
静默模式：关闭（可以看到反馈）
存储模式：追加模式（整合信息）
```

### 3. 测试和调试
```
运行模式：手动
智能多角色识别：启用
在"运行日志"标签页查看详细信息
```

## ⚠️ 注意事项

1. **API调用频率**
   - 智能模式会为每个角色调用一次API
   - 3个角色 = 3次API调用
   - 建议设置合理的触发阈值避免过度调用

2. **世界书条目**
   - 追加模式需要先存在角色信息条目
   - 可以使用"角色信息提取器"创建
   - 独立模式会自动创建新条目

3. **角色名称匹配**
   - 基于消息中的`name`字段
   - 确保角色名称准确一致
   - 不区分大小写的模糊匹配

4. **性能考虑**
   - 批量生成时每个角色间隔1秒
   - 5个角色大约需要5秒完成
   - 建议群聊不超过10个角色

## 🐛 故障排查

### 问题1：未检测到角色
**可能原因**：
- 最近20条消息中没有该角色发言
- 角色名称格式特殊（包含特殊字符）

**解决方法**：
- 增加对话长度后重试
- 使用手动模式指定角色名

### 问题2：日志保存失败
**可能原因**：
- 追加模式下角色条目不存在
- 世界书未正确加载

**解决方法**：
- 切换到独立模式
- 先使用"角色信息提取器"创建条目
- 检查角色是否绑定了世界书

### 问题3：API调用失败
**可能原因**：
- API配置不正确
- API额度不足
- 网络连接问题

**解决方法**：
- 检查API配置（URL、Key、模型）
- 查看"运行日志"标签页的详细错误
- 测试API连接

## 📝 日志示例

### 成功的批量生成日志
```
[批量日志] ========== 开始智能多角色日志生成 ==========
[角色识别] 在最近20条消息中检测到 3 个角色: 艾莉娅, 索菲亚, 艾米
[批量日志] 准备为 3 个角色生成日志
[批量日志] [1/3] 处理角色: 艾莉娅
[角色日志] ========== 开始生成角色日志 ==========
[角色日志] 目标角色: 艾莉娅
[角色日志] AI返回内容: 20251020 和{{user}}在咖啡厅度过了愉快的下午
[批量日志] ✓ 艾莉娅 - 成功
[批量日志] [2/3] 处理角色: 索菲亚
...
[批量日志] ========== 批量生成完成 ==========
[批量日志] 总计: 3 | 成功: 3 | 跳过: 0 | 失败: 0
```

## 🔄 版本历史

### v1.2.0 (2025-01-20)
- ✨ 新增智能多角色识别功能
- ✨ 新增静默模式
- 🔧 优化批量生成逻辑
- 📝 完善日志输出

---

**提示**：建议先在手动模式下测试功能，确认正常后再启用自动模式。
