# 角色日志功能说明

## 📖 功能概述

角色日志功能是自动剧情引擎的全新特性，可以自动记录角色在对话中经历的重要事件，并追加到角色的世界书条目末尾，形成一个持续增长的历史时间线。

## ✨ 主要特点

1. **智能事件提取** - AI自动分析最近的对话，识别对角色有重要意义的事件
2. **自动追加** - 新事件直接追加到现有角色条目，无需手动编辑
3. **时间线格式** - 使用标准化的日期+描述格式，易于阅读和管理
4. **第一人称视角** - 以角色自己的口吻记录事件，更具代入感
5. **可自定义提示词** - 可以调整AI生成日志的规则和风格

## 🎯 使用场景

- **长期RP记录** - 记录角色在长期对话中的成长和变化
- **关系发展追踪** - 记录角色与其他人物的关系进展
- **重要事件存档** - 自动保存关键剧情节点
- **角色回忆系统** - 为角色提供可查询的历史记忆

## 📝 使用步骤

### 前提条件

1. 角色必须已经存在世界书条目（可以使用"角色信息提取器"创建）
2. 已配置API设置（与剧情生成使用相同的API配置）

### 操作步骤

1. **打开设置面板**
   - 点击扩展面板中的"打开设置"按钮
   - 切换到"角色日志"标签页

2. **输入角色名称**
   - 在"角色名称"输入框中输入要记录日志的角色名称
   - 必须是已存在世界书条目的角色

3. **生成日志**
   - 点击"生成并追加日志"按钮
   - AI会分析最近10条对话消息
   - 自动生成并追加到角色条目末尾

4. **查看结果**
   - 打开世界书，找到对应角色的条目
   - 在条目末尾可以看到新增的"历史事件："部分
   - 每次生成会追加一条新记录

## 📊 日志格式示例

```
历史事件：
20250607 {{user}}送了我一个项链，我很喜欢
20250608 在咖啡厅遇到了老朋友
20250609 参加了公司年会，获得了最佳员工奖
```

## ⚙️ 自定义提示词

在"角色日志"标签页中，可以自定义AI生成日志的提示词：

### 默认提示词包含：
- 事件记录规则（只记录重要事件）
- 视角要求（第一人称）
- 格式要求（YYYYMMDD + 简短描述）
- 示例展示

### 可用变量：
- `${characterName}` - 自动替换为角色名称
- `${recentMessages}` - 自动替换为最近的对话内容

### 自定义建议：
- 调整事件重要性标准
- 修改描述风格（正式/口语/文学）
- 添加特定领域的记录要求
- 调整日期格式或添加其他元数据

## 🔧 技术细节

### 工作流程

1. **收集对话** - 获取最近10条消息
2. **构建提示词** - 使用自定义模板，替换变量
3. **调用AI** - 使用配置的API生成日志
4. **查找条目** - 在角色世界书中定位目标条目
5. **追加内容** - 在条目末尾添加新事件
6. **保存更新** - 保存世界书并刷新UI

### 条目查找逻辑

系统使用两种方法查找角色条目：

1. **优先方法** - 通过comment字段查找（角色提取器创建的条目）
   ```javascript
   entry.comment.includes(`角色信息: ${characterName}`)
   ```

2. **备用方法** - 通过关键词查找
   ```javascript
   entry.key.includes(characterName)
   ```

### API参数

```javascript
{
  temperature: 0.7,  // 适中的创造性
  max_tokens: 200,   // 足够生成一条简短记录
  stream: false      // 不使用流式输出
}
```

## 💡 最佳实践

### 使用建议

1. **定期生成** - 每次重要对话后手动触发一次
2. **检查结果** - 偶尔查看世界书确保格式正确
3. **清理旧记录** - 如果事件太多，可以手动精简
4. **配合角色提取器** - 先用提取器创建标准化的角色条目

### 注意事项

1. **避免频繁调用** - 每次调用都会消耗API额度
2. **输入正确名称** - 角色名称必须与世界书条目完全匹配
3. **保留格式** - 手动编辑时保持日期格式的一致性
4. **备份重要数据** - 定期导出世界书备份

### 常见问题

**Q: 提示"未找到角色的世界书条目"？**
A: 需要先使用"角色信息提取器"为该角色创建条目

**Q: 生成的日志格式不对？**
A: 检查自定义提示词是否正确，或恢复为默认提示词

**Q: 显示"没有重要事件"？**
A: 说明AI判断最近对话没有需要记录的内容，这是正常的

**Q: 可以删除某条历史记录吗？**
A: 可以，直接在世界书编辑器中手动删除或修改

## 🔮 未来计划

- [ ] 自动触发模式（每N条消息自动生成）
- [ ] 事件分类标签（如：relationship, achievement, conflict等）
- [ ] 批量生成（一次为多个角色生成日志）
- [ ] 日志搜索和过滤功能
- [ ] 可视化时间线展示
- [ ] 导出角色历史为独立文档

## 📞 技术支持

如遇到问题：
1. 查看"运行日志"标签页中的详细错误信息
2. 确认API配置正确
3. 检查角色条目是否存在
4. 查看控制台是否有报错信息

---

**版本**: 1.0.0  
**更新日期**: 2025-10-20  
**作者**: Auto Plot Engine Team
