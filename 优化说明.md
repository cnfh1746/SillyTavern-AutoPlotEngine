# 🚀 自动剧情引擎优化说明文档

## 📋 优化概览

本次优化共实施了**6项重要改进**，涵盖功能增强、稳定性提升和代码质量改善。

---

## ✅ 已完成的优化项目

### 优化1: 清理多余条目 🔧
**优先级**: 🔴 高  
**工作量**: 小  
**影响**: 防止条目累积

#### 实现位置
- `plot_engine.js` - `cleanupDuplicateEntries()` 函数

#### 功能描述
- 自动检测世界书中所有标记为"自动剧情大纲"的条目
- 按时间戳排序，保留最新的条目
- 删除所有旧的重复条目
- 确保世界书中始终只有一个有效的剧情大纲条目

#### 日志示例
```
[INFO] 发现 3 个剧情条目，保留最新的并删除旧条目
[INFO] 已删除旧条目 uid: 1
[INFO] 已删除旧条目 uid: 2
```

---

### 优化2: 触发关键词可配置化 ⚙️
**优先级**: 🟡 中  
**工作量**: 小  
**影响**: 提升灵活性

#### 实现位置
- `settingsManager.js` - 添加 `entryKeywords` 配置
- `plot_engine.js` - 使用配置的关键词
- `settings.html` - UI配置界面

#### 功能描述
- 用户可在设置界面自定义触发关键词
- 支持逗号分隔输入多个关键词
- 默认值：`["剧情", "plot", "大纲", "故事", "情节"]`
- 实时保存和应用

#### 使用方法
1. 打开设置面板
2. 找到"世界书条目配置" → "触发关键词"
3. 输入自定义关键词，用逗号分隔
4. 保存后立即生效

---

### 优化3: 深度和位置可配置化 📍
**优先级**: 🟡 中  
**工作量**: 小  
**影响**: 提升灵活性

#### 实现位置
- `settingsManager.js` - 添加 `entryPosition` 和 `entryDepth` 配置
- `plot_engine.js` - 使用配置的位置和深度
- `settings.html` - UI配置界面

#### 功能描述
**位置选项**：
- `0` - 📌 角色定义之前 (Before Char)
- `1` - 📌 角色定义之后 (After Char)
- `2` - ⬆️ 顶部 (Top)
- `3` - ⬇️ 底部 (Bottom)
- `4` - ⚙️ 在指定深度 (@ Depth) **[默认推荐]**

**深度设置**：
- 范围：0-999
- 默认：1
- 说明：深度1最靠近对话，数字越大越远离对话

#### 推荐配置
```
位置：⚙️ 在指定深度 (@ Depth)
深度：1
```
显示为：**蓝灯@齿轮D1**

---

### 优化4: 内容去重检测 🔍
**优先级**: 🟡 中  
**工作量**: 小  
**影响**: 减少无意义更新

#### 实现位置
- `plot_engine.js` - `updatePlotLorebook()` 函数

#### 功能描述
- 更新前对比新旧内容
- 如果内容完全相同，跳过更新
- 避免无意义的世界书写入
- 减少日志噪音和UI刷新

#### 日志示例
```
[INFO] 找到现有条目 (uid: 0)，正在更新...
[INFO] 剧情内容未变化，跳过更新
```

#### 用户提示
```
剧情大纲内容未变化
```

---

### 优化5: 保留历史版本 📚
**优先级**: 🟡 中  
**工作量**: 中  
**影响**: 便于回溯

#### 实现位置
- `plot_engine.js` - `manageHistoryVersions()` 和 `createHistoryEntry()` 函数
- `CONSTANTS.MAX_HISTORY_VERSIONS = 5`

#### 功能描述
- 每次更新前自动保存旧版本
- 最多保留5个历史版本（可在常量中调整）
- 超过限制时自动删除最旧的版本
- 历史版本默认禁用，不会干扰对话

#### 条目结构
```
注释: "剧情大纲历史版本 #1 - 2025-01-14T19:30:00.000Z"
关键词: ["历史版本1", "history_v1"]
状态: 禁用 (disable=true)
深度: depth+1 (比主条目深1层)
```

#### 日志示例
```
[INFO] 当前历史版本数: 4
[SUCCESS] 已创建历史版本 #5 (uid: 123)
[INFO] 已删除历史版本 uid: 100 (最旧的版本)
```

#### 查看历史版本
1. 打开世界书编辑器
2. 查找注释为"剧情大纲历史版本"的条目
3. 如需恢复旧版本，可手动启用并复制内容

---

### 优化6: API重试机制 🔄
**优先级**: 🟡 中  
**工作量**: 中  
**影响**: 提升稳定性

#### 实现位置
- `plot_engine.js` - `fetchWithRetry()` 函数
- `CONSTANTS.API_RETRY_ATTEMPTS = 3`
- `CONSTANTS.API_RETRY_DELAY = 2000` (毫秒)

#### 功能描述
- API调用失败时自动重试最多3次
- 指数退避策略：第1次等待2秒，第2次等待4秒，第3次等待6秒
- 针对以下错误自动重试：
  - 服务器错误 (5xx)
  - 限流错误 (429)
  - 网络连接错误

#### 日志示例
```
[INFO] API调用尝试 1/3
[WARN] API返回错误 500，2000ms后重试...
[INFO] API调用尝试 2/3
[WARN] 网络错误，4000ms后重试: Connection timeout
[INFO] API调用尝试 3/3
[SUCCESS] API调用成功，生成内容长度: 1973 字符
```

#### 好处
- 网络波动时提升成功率
- 服务器临时错误时自动恢复
- 减少人工干预需求

---

### 优化7: 常量提取 📝
**优先级**: 🟢 低  
**工作量**: 小  
**影响**: 提升可维护性

#### 实现位置
- `plot_engine.js` - `CONSTANTS` 对象

#### 常量列表
```javascript
const CONSTANTS = {
    ENTRY_COMMENT: "自动剧情大纲",
    HISTORY_COMMENT: "剧情大纲历史版本",
    MAX_ALLOWED_TOKENS: 65536,
    MAX_HISTORY_VERSIONS: 5,
    API_RETRY_ATTEMPTS: 3,
    API_RETRY_DELAY: 2000,
    DEFAULT_KEYWORDS: ["剧情", "plot", "大纲", "故事", "情节"],
    DEFAULT_POSITION: 4,
    DEFAULT_DEPTH: 1,
    DEFAULT_ORDER: 100,
    DEFAULT_PROBABILITY: 100,
};
```

#### 好处
- 集中管理魔法数字和字符串
- 便于后续调整参数
- 提高代码可读性
- 减少硬编码

---

### 优化8: 函数职责分离 🔨
**优先级**: 🟢 低  
**工作量**: 大  
**影响**: 提升代码质量

#### 实现位置
- `plot_engine.js` - 多个工具函数

#### 新增函数

##### 1. `validateApiConfig(settings)`
验证API配置完整性
```javascript
// 检查必需字段：apiUrl, apiKey, model
// 返回布尔值表示配置是否有效
```

##### 2. `normalizeApiUrl(apiUrl)`
规范化API URL
```javascript
// 去除尾部斜杠
// 自动添加 /chat/completions 后缀
```

##### 3. `limitTokens(maxTokens)`
限制token数量在合理范围内
```javascript
// 最小值：100
// 最大值：65536
// 超出时自动调整并记录日志
```

##### 4. `buildApiRequestBody(settings, prompt)`
构建API请求体
```javascript
// 组装完整的请求参数
// 处理类型转换
// 返回标准格式对象
```

##### 5. `extractTimestamp(comment)`
从注释中提取时间戳
```javascript
// 解析 "xxx - ISO8601" 格式
// 返回Date对象
```

##### 6. `findMatchingEntries(entries, commentPrefix)`
查找匹配的条目
```javascript
// 按注释前缀筛选
// 返回条目数组
```

##### 7. `configureEntryProperties(entry, settings)`
配置条目属性
```javascript
// 设置关键词、位置、深度
// 启用条目
// 统一配置逻辑
```

##### 8. `getCurrentLorebook()`
获取当前角色的主世界书
```javascript
// 调用TavernHelper API
// 错误处理和验证
```

##### 9. `triggerUIRefresh(lorebookName)`
触发UI刷新
```javascript
// 发送多个刷新事件
// 确保UI同步
```

#### 好处
- 单一职责原则
- 代码复用性高
- 便于单元测试
- 提高可读性

---

### 优化9: 类型安全增强 🛡️
**优先级**: 🟢 低  
**工作量**: 中  
**影响**: 减少错误

#### 实现位置
- `plot_engine.js` - JSDoc类型注释

#### 类型定义

##### WorldBookEntry
```javascript
/**
 * @typedef {Object} WorldBookEntry
 * @property {number} uid - 条目唯一ID
 * @property {string} comment - 条目注释
 * @property {string} content - 条目内容
 * @property {string[]} key - 触发关键词数组
 * @property {boolean} constant - 是否为常驻条目
 * @property {number} selectiveLogic - 选择逻辑
 * @property {number} position - 插入位置
 * @property {number} depth - 插入深度
 * @property {boolean} disable - 是否禁用
 * @property {boolean} enabled - 是否启用
 * @property {number} order - 排序顺序
 * @property {number} probability - 触发概率
 */
```

##### ApiSettings
```javascript
/**
 * @typedef {Object} ApiSettings
 * @property {string} apiUrl - API地址
 * @property {string} apiKey - API密钥
 * @property {string} model - 模型名称
 * @property {number} maxTokens - 最大token数
 * @property {number} temperature - 温度参数
 * @property {number} topP - Top P参数
 * @property {number} presencePenalty - 存在惩罚
 * @property {number} frequencyPenalty - 频率惩罚
 * @property {string} plotMasterPrompt - 核心提示词
 * @property {string[]} entryKeywords - 触发关键词
 * @property {number} entryPosition - 插入位置
 * @property {number} entryDepth - 插入深度
 */
```

#### 函数注释示例
```javascript
/**
 * 验证API配置的完整性
 * @param {ApiSettings} settings - API设置对象
 * @returns {boolean} 配置是否完整
 */
function validateApiConfig(settings) { ... }
```

#### 好处
- IDE智能提示
- 参数类型检查
- 减少类型错误
- 提升开发效率
- 更好的文档

---

## 📊 优化效果对比

### 优化前
```
❌ 多次运行产生多个重复条目
❌ 关键词、位置、深度硬编码
❌ API失败立即放弃
❌ 无历史版本管理
❌ 函数职责混乱，难以维护
❌
