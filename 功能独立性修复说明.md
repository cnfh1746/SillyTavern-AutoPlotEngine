# 功能独立性修复说明

## 修复时间
2025/10/20

## 问题描述

之前的版本存在以下问题：
1. **剧情大纲和角色日志功能不独立**：核心设置中的"运行模式"同时控制两个功能
2. **角色日志自动模式不生效**：只有剧情大纲在计数，角色日志没有独立计数
3. **enabled被当作核心开关**：关闭后两个功能都无法使用

## 修复内容

### 1. UI结构调整 (modal_settings.html)

#### 核心设置标签页
- 只保留**静默模式**（全局设置）
- 移除了运行模式和触发阈值设置

#### 剧情大纲标签页
新增独立的基本设置区域：
- ✅ **启用剧情大纲**：独立的功能开关
- ✅ **运行模式**：自动/手动（只控制剧情大纲）
- ✅ **自动触发阈值**：剧情大纲的触发阈值
- ✅ **立即生成按钮**：手动模式专用

#### 角色日志标签页
已有独立的基本设置区域：
- ✅ **启用角色日志**：独立的功能开关
- ✅ **运行模式**：自动/手动（只控制角色日志）
- ✅ **自动触发阈值**：角色日志的触发阈值
- ✅ **立即生成按钮**：手动模式专用

### 2. 事件处理修复 (index.js)

**问题根源**：
index.js中有旧的事件处理逻辑与events.js中的新逻辑冲突

**修复方案**：
```javascript
// 旧代码（已删除）
let messageCounter = 0;
async function onMessageReceived() {
    // 旧的单一计数器逻辑
}

// 新代码
import { onMessageReceived, onChatChanged } from './core/events.js';
// 使用events.js中的独立计数器逻辑
```

### 3. 独立计数器 (events.js)

已正确实现两个独立的消息计数器：

```javascript
let plotMessageCount = 0;    // 剧情大纲计数器
let diaryMessageCount = 0;   // 角色日志计数器
```

每个功能有独立的触发逻辑：
- **剧情大纲**：检查 `settings.enabled` 和 `settings.triggerThreshold`
- **角色日志**：检查 `settings.diaryEnabled` 和 `settings.diaryTriggerThreshold`

### 4. UI显示逻辑 (settingsManager.js)

添加了独立的UI控制函数：
- `updatePlotUIVisibility()`：控制剧情大纲标签页的UI
- `updateDiaryUIVisibility()`：控制角色日志标签页的UI

## 功能特性

### ✅ 完全独立运行
- 剧情大纲和角色日志可以单独开启
- 也可以同时开启
- 互不影响

### ✅ 独立的运行模式
- 剧情大纲可以是自动，角色日志可以是手动
- 或者反过来
- 或者都是自动/手动

### ✅ 独立的触发阈值
- 剧情大纲：默认10条消息
- 角色日志：默认15条消息
- 可以各自调整

### ✅ 独立的计数器
- 每个功能有自己的消息计数
- 切换聊天时两个计数器都会重置
- 控制台会显示详细的计数日志

## 测试建议

### 测试场景1：只开启剧情大纲
1. 启用剧情大纲，设置自动模式，阈值2
2. 禁用角色日志
3. 发送2条消息
4. 应该看到：剧情计数到2并生成，日志计数无变化

### 测试场景2：只开启角色日志
1. 禁用剧情大纲
2. 启用角色日志，设置自动模式，阈值2
3. 发送2条消息
4. 应该看到：日志计数到2并生成，剧情计数无变化

### 测试场景3：同时开启
1. 启用剧情大纲，阈值3
2. 启用角色日志，阈值5
3. 发送消息
4. 应该看到：
   - 3条消息时：剧情生成，剧情计数重置
   - 5条消息时：日志生成，日志计数重置

## 预期的控制台日志

开启两个功能后，每条消息应该看到：
```
[INFO] [主引擎] 剧情消息计数: 1/3
[INFO] [主引擎] 日志消息计数: 1/5
```

达到阈值时：
```
[INFO] [主引擎] 达到剧情生成阈值，自动触发...
[SUCCESS] [主引擎] 自动剧情生成完成，计数器已重置
```

或
```
[INFO] [主引擎] 达到日志生成阈值，自动触发...
[SUCCESS] [主引擎] 自动日志生成完成，计数器已重置
```

## 重要提示

⚠️ **必须重启SillyTavern**才能使修改生效！

刷新页面或重新加载扩展可能不够，建议：
1. 完全关闭SillyTavern
2. 重新启动
3. 检查设置是否正确加载
4. 开始测试

## 文件清单

修改的文件：
- ✅ `modal_settings.html` - UI结构调整
- ✅ `core/settingsManager.js` - UI显示逻辑
- ✅ `index.js` - 使用新的事件处理

未修改的文件（已经是正确的）：
- ✅ `core/events.js` - 独立的计数器逻辑
- ✅ `core/character_diary.js` - 角色日志功能
- ✅ `core/plot_engine.js` - 剧情大纲功能
