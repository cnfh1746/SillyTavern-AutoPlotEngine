# 角色日志功能更新说明 v2.0

## 🎉 更新概述

角色日志功能现已全面升级！新版本提供了更灵活的运行模式和存储选项，让您可以更好地管理角色的历史记录。

## ✨ 主要新增功能

### 1. 双运行模式 🔄

与剧情大纲功能类似，角色日志现在支持两种运行模式：

#### 🤖 自动模式
- **自动触发**：每当有N条新消息后，自动为指定角色生成日志
- **可配置阈值**：默认15条消息，可自定义（5-50条）
- **目标角色设置**：可指定特定角色，留空则使用当前对话角色
- **后台运行**：无需手动操作，自动记录角色经历

#### 👆 手动模式
- **按需触发**：需要时点击按钮生成
- **灵活控制**：完全掌控何时记录日志
- **即时反馈**：立即查看生成结果

### 2. 双存储模式 📚

现在可以选择日志的存储位置：

#### 📝 追加模式（默认）
- **追加到原条目**：在现有角色条目末尾添加"历史事件"部分
- **集中管理**：所有角色信息（基本信息+历史事件）在同一条目
- **适合场景**：简洁的角色管理，信息集中查看

**示例**：
```
# 角色名

基本信息...

历史事件：
20250607 {{user}}送了我一个项链，我很喜欢
20250608 在咖啡厅遇到了老朋友
```

#### 📖 独立模式
- **创建新条目**：创建独立的"角色名日志"条目
- **自动触发**：触发词自动设置为"角色全名"和"简称"（前2个字）
- **绿灯状态**：条目默认启用，优先级100
- **持续追加**：后续日志自动追加到此独立条目
- **适合场景**：长期RP记录，需要独立管理角色历史

**示例条目**：
```
条目名：张三日志
触发词：["张三", "张三"]
状态：✅ 已启用
位置：Before Char

内容：
# 张三日志

20250607 {{user}}送了我一个项链，我很喜欢
20250608 在咖啡厅遇到了老朋友
20250609 参加了公司年会
```

## ⚙️ 新增配置选项

### 基本设置
- ✅ **启用角色日志**：总开关，关闭后所有日志功能不运行
- 🔄 **运行模式**：选择"自动"或"手动"
- 🎯 **自动触发阈值**：设置每N条消息触发一次（仅自动模式）
- 👤 **目标角色**：指定要记录的角色名称（仅自动模式）

### 存储设置
- 📍 **存储位置**：选择"追加到原角色条目"或"创建独立日志条目"

### 提示词设置
- 📝 **自定义提示词**：可调整AI生成日志的规则和风格
- 🔧 **变量支持**：`${characterName}`, `${recentMessages}`

## 📋 使用指南

### 首次使用

1. **打开设置面板**
   - 点击扩展面板中的"打开设置"按钮
   - 切换到"角色日志"标签页

2. **启用功能**
   - 打开"启用角色日志"开关

3. **配置API**（如果还没配置）
   - 切换到"API配置"标签
   - 填写API URL、API Key和模型名称

### 自动模式设置

1. **选择运行模式**
   - 在"运行模式"中选择"自动 (每N条消息)"

2. **设置触发阈值**
   - 默认15条消息，根据需要调整

3. **指定目标角色**（可选）
   - 输入角色名称，例如："张三"
   - 留空则使用当前对话角色

4. **选择存储模式**
   - 追加模式：适合简洁管理
   - 独立模式：适合长期记录

5. **保存设置**
   - 点击"保存设置"按钮
   - 系统将自动开始监控消息数

### 手动模式使用

1. **选择运行模式**
   - 在"运行模式"中选择"手动"

2. **选择存储模式**
   - 根据需要选择追加或独立模式

3. **使用时**
   - 在"角色名称"输入框输入角色名
   - 点击"立即生成日志"按钮
   - 等待AI分析并生成日志

## 🔧 技术细节

### 自动触发逻辑
- 监听消息事件
- 计数新消息
- 达到阈值时自动触发
- 重置计数器

### 独立模式条目结构
```javascript
{
  uid: auto_increment,
  key: [角色全名, 角色简称],  // 自动生成触发词
  comment: "角色日志: 角色名",
  content: "# 角色名日志\n\n日志内容...",
  enabled: true,
  selective: true,
  position: 0,  // before_char
  insertion_order: 100,
  // ... 其他配置
}
```

### 条目查找优先级
1. 通过comment查找（`角色信息: 角色名`或`角色日志: 角色名`）
2. 通过key关键词查找
3. 如果不存在则创建（仅独立模式）

## 💡 最佳实践

### 模式选择建议

**使用自动模式**：
- ✅ 长期稳定的RP对话
- ✅ 主要角色的持续记录
- ✅ 希望无缝记录所有事件
- ⚠️ 注意API消耗

**使用手动模式**：
- ✅ 临时对话或测试场景
- ✅ 希望精确控制记录时机
- ✅ API额度有限
- ✅ 只记录重要对话

**使用追加模式**：
- ✅ 角色条目较少
- ✅ 希望信息集中管理
- ✅ 不需要独立查看历史

**使用独立模式**：
- ✅ 长期RP项目
- ✅ 需要频繁查看历史
- ✅ 历史事件较多
- ✅ 希望保持角色条目简洁

### 阈值设置建议

| 对话类型 | 建议阈值 | 说明 |
|---------|---------|------|
| 密集对话 | 20-30条 | 避免过于频繁调用 |
| 普通对话 | 15条（默认） | 平衡记录频率和覆盖度 |
| 缓慢对话 | 10条 | 确保不遗漏重要事件 |
| 测试模式 | 5条 | 快速验证功能 |

## 🆚 模式对比

| 特性 | 自动模式 | 手动模式 |
|------|----------|----------|
| 触发方式 | 自动 | 手动 |
| API消耗 | 较高 | 可控 |
| 便利性 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| 控制精度 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 适合场景 | 长期RP | 临时对话 |

| 特性 | 追加模式 | 独立模式 |
|------|----------|----------|
| 条目数量 | 不增加 | +1 |
| 信息集中度 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| 历史独立性 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 触发灵活性 | 依赖原条目 | 独立触发词 |
| 适合场景 | 简洁管理 | 长期项目 |

## 🐛 常见问题

**Q: 自动模式不工作？**
A: 检查以下几点：
1. "启用角色日志"开关是否打开
2. 是否选择了"自动"模式
3. API配置是否正确
4. 是否已达到触发阈值

**Q: 独立模式创建的条目触发词是什么？**
A: 自动设置为[角色全名, 角色简称(前2字)]

**Q: 可以同时使用两种存储模式吗？**
A: 不可以，每次只能使用一种模式。但可以随时切换。

**Q: 切换存储模式后之前的日志会怎样？**
A: 已生成的日志不受影响，新日志将使用新的存储模式。

## 🔮 未来计划

- [ ] 多角色批量生成
- [ ] 事件分类标签
- [ ] 智能触发（根据对话内容重要性）
- [ ] 可视化时间线
- [ ] 日志导出功能
- [ ] 历史事件搜索

## 📞 反馈与支持

如有问题或建议，请：
1. 查看"运行日志"标签页的详细信息
2. 检查浏览器控制台错误
3. 确认世界书条目状态

---

**版本**: 2.0.0  
**更新日期**: 2025-10-20  
**兼容性**: SillyTavern 1.12.0+
