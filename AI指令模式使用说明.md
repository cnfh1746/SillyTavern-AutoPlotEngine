# AI指令模式使用说明

## 功能概述

角色日志生成器现在支持两种工作模式：

### 1️⃣ 传统角色名称模式
直接输入角色名称，为单个角色生成日志。

**示例：**
```
小明
```

### 2️⃣ AI指令模式 ⭐ 新功能
输入自然语言指令，让AI智能分析对话内容并批量处理多个角色的日志。

**示例：**
```
请读取对话中的表格，为表格中提到的所有角色生成日志
```

## AI指令模式工作流程

```
用户输入指令
    ↓
系统检测指令关键词（请、分析、表格、批量等）
    ↓
调用AI分析对话内容
    ↓
AI识别需要生成日志的角色列表
    ↓
系统批量为每个角色生成日志
    ↓
显示完成统计（成功/跳过/失败）
```

## 触发条件

系统会自动检测以下情况并启用AI指令模式：

1. **输入长度超过20字符**
2. **包含关键词：**
   - `请`
   - `分析`
   - `表格`
   - `所有角色`
   - `批量`
   - `生成日志`
   - `读取`

## 使用场景

### 场景1：从表格批量生成日志
当对话中包含角色状态表格时，可以让AI读取表格并为所有角色生成日志。

**输入示例：**
```
请读取最近对话中的表格，为表格中的所有角色生成日志
```

**AI会做什么：**
1. 分析最近30条对话
2. 识别表格中的角色名称
3. 为每个角色生成对应的事件日志

### 场景2：为对话中的所有角色生成日志
无需手动输入每个角色名称，让AI自动识别。

**输入示例：**
```
分析对话，为参与对话的所有角色生成日志
```

### 场景3：根据特定条件生成日志
可以给AI更具体的筛选条件。

**输入示例：**
```
请为对话中发生重要事件的角色生成日志
```

```
分析最近的剧情，为主要参与者生成日志记录
```

## 实际使用示例

### 示例1：表格场景

**对话内容包含：**
```
角色状态表：
- 小明：获得神秘宝石，等级提升
- 小红：学会新技能"火焰术"
- 老王：完成主线任务，获得称号
```

**输入指令：**
```
请读取表格，为所有角色生成日志
```

**AI处理过程：**
```
[AI指令模式] 用户指令: 请读取表格，为所有角色生成日志
[AI指令模式] AI识别到 3 个角色: 小明, 小红, 老王
[AI指令模式] 原因: 表格中列出了3个角色及其状态变化
[AI指令模式] [1/3] 处理角色: 小明
[AI指令模式] ✓ 小明 - 成功
[AI指令模式] [2/3] 处理角色: 小红
[AI指令模式] ✓ 小红 - 成功
[AI指令模式] [3/3] 处理角色: 老王
[AI指令模式] ✓ 老王 - 成功
[AI指令模式] 批量生成完成
[AI指令模式] 总计: 3 | 成功: 3 | 跳过: 0 | 失败: 0
```

### 示例2：简单场景

**对话内容：**
```
User: 小明和小红一起去冒险
AI: 他们在森林里遇到了怪物
```

**输入指令：**
```
为所有角色生成日志
```

**AI处理过程：**
```
[AI指令模式] AI识别到 2 个角色: 小明, 小红
[AI指令模式] 批量处理中...
[AI指令模式] 成功: 2 | 跳过: 0 | 失败: 0
```

## 优势对比

### 传统模式
- ❌ 需要逐个输入角色名称
- ❌ 表格中有10个角色需要点击10次
- ❌ 容易遗漏角色
- ✅ 精确控制单个角色

### AI指令模式
- ✅ 一次性处理多个角色
- ✅ 自动识别表格内容
- ✅ 批量处理效率高
- ✅ 智能理解用户意图
- ⚠️ 需要额外的API调用

## 注意事项

### 1. API消耗
AI指令模式会额外调用一次API来分析指令，然后为每个角色再调用一次API。

**示例消耗：**
- 1次指令分析调用
- N次角色日志生成调用（N = 识别到的角色数量）
- 总计：1 + N 次API调用

### 2. 延迟控制
系统会在每个角色处理之间添加1秒延迟，避免API请求过快。

### 3. 错误处理
- 如果AI无法识别角色，会返回空列表并说明原因
- 单个角色失败不影响其他角色的处理
- 最终会显示详细的成功/跳过/失败统计

### 4. 静默模式
批量处理时会强制使用静默模式，避免频繁弹出通知。完成后会显示一次汇总通知。

## 控制台日志示例

### 成功案例
```
[INFO] [角色日志] 检测到AI指令模式，切换到AI指令处理...
[INFO] [AI指令模式] ========== 开始处理AI指令 ==========
[INFO] [AI指令模式] 用户指令: 请读取表格为所有角色生成日志
[INFO] [AI指令模式] 正在调用AI分析指令...
[INFO] [AI指令模式] AI响应: {"characters":["小明","小红","老王"],"reason":"表格中明确列出了这3个角色"}
[INFO] [AI指令模式] AI识别到 3 个角色: 小明, 小红, 老王
[INFO] [AI指令模式] 原因: 表格中明确列出了这3个角色
[INFO] [AI指令模式] [1/3] 处理角色: 小明
[SUCCESS] [AI指令模式] ✓ 小明 - 成功
[INFO] [AI指令模式] [2/3] 处理角色: 小红
[SUCCESS] [AI指令模式] ✓ 小红 - 成功
[INFO] [AI指令模式] [3/3] 处理角色: 老王
[SUCCESS] [AI指令模式] ✓ 老王 - 成功
[SUCCESS] [AI指令模式] ========== 批量生成完成 ==========
[INFO] [AI指令模式] 总计: 3 | 成功: 3 | 跳过: 0 | 失败: 0
```

### 无角色案例
```
[INFO] [AI指令模式] AI识别到 0 个角色
[INFO] [AI指令模式] 原因: 对话内容过于简短，无法识别明确的角色信息
[INFO] [AI指令模式] 没有需要生成日志的角色
```

## 推荐用法

### ✅ 推荐
```
请读取表格，为表格中的所有角色生成日志
分析对话，为所有角色生成日志
为最近参与对话的角色生成日志
```

### ⚠️ 不推荐
```
小明（太短，会被识别为角色名称模式）
生成（太模糊）
```

## 与智能多角色识别的区别

| 特性 | 智能多角色识别 | AI指令模式 |
|------|--------------|-----------|
| 触发方式 | 自动触发 | 手动触发 |
| 角色识别 | 代码解析对话 | AI分析对话 |
| 控制性 | 无法控制 | 可通过指令控制 |
| 灵活性 | 固定逻辑 | 自然语言指令 |
| 适用场景 | 自动化工作流 | 特殊场景处理 |

## 最佳实践

1. **表格场景优先使用AI指令模式**
   - 表格格式多样，AI能更好理解
   
2. **指令要清晰明确**
   - ✅ "请读取对话中的表格，为所有角色生成日志"
   - ❌ "帮我处理一下"

3. **适时使用传统模式**
   - 只需要处理1-2个角色时，直接输入名称更快

4. **结合智能多角色识别**
   - 自动模式：启用智能多角色识别
   - 手动模式：使用AI指令处理特殊情况

## 故障排除

### 问题1：AI没有识别到角色
**原因：** 对话内容不够清晰
**解决：** 使用更具体的指令，如"请读取对话中的角色状态表"

### 问题2：识别到错误的角色
**原因：** AI误判
**解决：** 改用传统模式手动输入正确的角色名

### 问题3：处理失败
**原因：** 世界书条目不存在
**解决：** 先使用"角色信息提取器"创建角色条目

## 更新日志

### 2025/10/21
- ✅ 新增AI指令模式
- ✅ 支持自然语言批量处理
- ✅ 智能识别关键词触发
- ✅ 批量处理进度显示
- ✅ 详细的成功/失败统计

---

💡 **小贴士：** AI指令模式特别适合处理包含大量角色表格的场景，可以大大提高工作效率！
